<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button x:Name="changeText" Content="Kliknięcie zmienia etykietę" HorizontalAlignment="Center" Click="changeText_Click" VerticalAlignment="Center"/>

        <CheckBox x:Name="enableCheckbox" Content="Włącza zmianę etykienty" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="True" Grid.Column="1"/>

        <TextBlock x:Name="labelToChange" Grid.Row="1" TextWrapping="Wrap" Text="Nacisnij przycisk, aby zmienić tekst" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="2"/>


    </Grid>
</Window>
